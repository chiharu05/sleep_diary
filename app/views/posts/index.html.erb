<h1>今月の記録</h1>
  <%= month_calendar events: @posts do |date,posts| %>
    <%= date.day %>
      <% posts.each do|post| %>
          <div>
          <%= post.kind.name %>
          </div>
      <% end %>
  <% end %>

<%= link_to "投稿ページに戻る", new_post_path, class:"post" %>